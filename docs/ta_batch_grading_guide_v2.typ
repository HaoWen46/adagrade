// ta_batch_grading_guide_v2.typ — 助教批改流程 v2.1（AdaGrade 版，前身 ADA-Marker）
// 紅字 = 相對 v1.0（ChatGPT 網頁流程）新增或改寫；黑字 = 沿用 v1.0。
// 編譯：typst compile docs/ta_batch_grading_guide_v2.typ docs/ta_batch_grading_guide_v2.pdf

#set document(
  title: "180 份考卷批次批改與 PDF 產出流程說明（v2.1，AdaGrade 版）",
  author: "ADA 課程助教",
)

#let R = rgb("#c00000")
#let chg(body) = text(fill: R, body)

#set page(paper: "a4", margin: (x: 2.4cm, top: 2.6cm, bottom: 2.5cm))
#set text(font: ("New Computer Modern", "Songti TC"), size: 10.5pt, lang: "zh", region: "TW")
#set par(justify: true, leading: 0.74em)
#set heading(numbering: "1.1")
#show heading.where(level: 1): it => block(above: 1.5em, below: 0.85em, text(size: 14pt, it))
#show heading.where(level: 2): it => block(above: 1.25em, below: 0.65em, text(size: 11.5pt, it))
#show raw: set text(font: ("DejaVu Sans Mono", "Songti TC"), size: 9pt)

#let callout(title, body, tone: luma(150), fillc: luma(248)) = block(
  width: 100%,
  breakable: false,
  fill: fillc,
  stroke: (left: 2.5pt + tone, rest: 0.4pt + luma(205)),
  inset: (x: 11pt, y: 9pt),
  radius: 2pt,
)[#text(weight: "bold")[#title] #v(3pt) #body]

#let tstroke = 0.4pt + luma(150)

// ============================ 封面 ============================
#v(3.2cm)
#align(center)[
  #text(size: 20pt, weight: "bold")[180 份考卷批次批改與 PDF 產出流程說明]
  #v(0.5em)
  #text(size: 12pt)[給助教參考的標準作業流程#chg[（AdaGrade 版）]]
]
#v(1.6em)
本文件說明如何將全班學生的#chg[掃描考卷匯入課程自架的 AdaGrade 系統]，依同一份
rubric 進行#chg[AI 輔助]批改#chg[、人工複核與發布]，並為每位學生產出完整的批改
PDF 與成績彙整#chg[（由系統寄送至學生信箱）]。

#v(1.2em)
#grid(
  columns: (5.5em, 1fr),
  row-gutter: 0.55em,
  [*版本：*], [#chg[v2.1（取代 v1.0 的 ChatGPT 網頁流程）]],
  [*日期：*], [#chg[2026-07-20]],
  [*適用情境：*], [單題或多題手寫答案批改、逐字轉錄、#chg[AI 輔助評分、強制人工抽查、發布與 regrade]],
  [*系統名稱：*], [#chg[AdaGrade（前身 ADA-Marker；伺服器指令與環境變數仍沿用 `adamarker`／`ADAMARKER_*` 舊名）]],
)

#v(1.4em)
#callout([#chg[閱讀說明]], tone: R, fillc: rgb("#fdf3f3"))[
  #chg[紅字為相對 v1.0（ChatGPT 網頁流程）*新增或改寫*的內容；黑字為沿用 v1.0
  的原則與規範。v1.0 原始文件保留於 `docs/ta_batch_grading_guide_v1.pdf`
  供對照。]
]

// ============================ 目錄 ============================
#set page(
  header: [
    #set text(size: 9pt)
    180 份考卷批次批改流程#h(1fr)助教參考文件#chg[（v2.1）]
    #v(-4pt)
    #line(length: 100%, stroke: 0.4pt)
  ],
  footer: context align(center, text(size: 9pt, counter(page).display("1/1", both: true))),
)
#counter(page).update(1)

#outline(title: [目錄], depth: 2, indent: auto)
#pagebreak(weak: true)

// ============================ 1 ============================
= 核心原則

#callout([一句話摘要])[
  不要一開始就處理 180 份。先用 5–10 份代表性答案做校準，確認轉錄保真度、批改風格、評語密度與配分標準後，#chg[再對全班一次啟動正式批改——正式批改不再需要人工分批。]
]

建議把整個工作視為一個「可稽核的批次批改流程」，而不是單次對話中的大量檔案處理。
#chg[在 v2 中，可稽核性由系統內建：每一筆批改紀錄自動保留原始掃描頁、遮罩後影像、逐字轉錄、逐條給分與評語、API 成本，以及當時使用的 rubric 版本與批改方法版本，事後隨時可回查。]

#callout([最重要的提醒])[
  手寫答案批改必須避免「幫學生補完答案」。轉錄時應保留學生原始用詞、錯字、英文文法錯誤、符號錯誤與推導跳躍。若字跡不清，應明確標記為不確定，而不是猜成正確答案。
  #chg[此規則已寫入系統內建的批改模板：模型被要求逐字轉錄（數學以 LaTeX 表示）、禁止補寫，字跡無法辨認時必須將該筆 confidence 標為 `illegible`，而非自行猜測。]
]

#chg[與 v1 最大的差異：批改不再經過 ChatGPT 網頁。AdaGrade 是課程自架的服務，考卷影像先遮罩學生身分才會送往視覺模型，全流程在自己的主機與課程控制的 API
金鑰之間完成，學生資料不會進入任何第三方的對話介面或檔案庫。]

// ============================ 2 ============================
= 檔案整理方式

== 建議檔名

#chg[不再需要逐題裁切影像或以匿名編號逐一命名（v1 的 `Q5_001.png` 命名法作廢）。依掃描方式擇一路徑：]

- #chg[*逐生 PDF（Submissions 分頁）*：檔案已按學生分開時使用。每位學生一個
  `<student_id>.pdf`（檔名即學號），第 _i_ 頁對應第 _i_ 題。檔名比對不到名冊的檔案會進入隔離區（quarantine），由助教手動指派。]
- #chg[*整疊掃描（Identify 分頁）*：考卷成疊過掃描機時使用。答案頁需有三個手寫表頭欄位——學號、姓名、題號（如 `Q1`）。散頁順序不拘，整疊掃成一個或多個 PDF
  （可多 GB）或圖片 zip 直接上傳，系統逐頁辨識並歸位到「學生 × 題目」矩陣。]

#chg[不需要預先裁切題目區域：系統以「頁」為處理單位，依題號欄位自動歸位，批改時模型看到的是遮罩後的整頁影像。]

== 建議 roster 表格

另外準備一份名冊 CSV。#chg[格式為 UTF-8、表頭固定三欄
`student_id,name,email`（Excel 請用「CSV UTF-8」另存），於系統的 *Students*
頁匯入。重複匯入採更新合併（upsert），不會自動刪除任何人；加退選後重新匯入即可，退選需在差異提示中明確按下 Withdraw 確認。]

#align(center)[
```
student_id,name,email
b11902001,王小明,b11902001@ntu.edu.tw
b11902002,李小華,b11902002@ntu.edu.tw
```
]

學生真實姓名與作答影像的對應#chg[由系統持有並隔離——視覺模型只看得到遮罩後影像，看不到學號與姓名（見第 7 節），因此不再需要另外維護「匿名編號對照表」。]

// ============================ 3 ============================
= 建議工作流程

#chg[*開始前的一次性設定（v2 新增）*：（1）*Providers* 頁加入 API 金鑰與要用的模型，並填入每百萬 token 單價——單價驅動成本估算與月度預算煞車；（2）*Methods*
頁建立批改方法（供應商 ＋ 模型 ＋ 提示模板 ＋ 寬嚴政策 lenient / standard /
strict）；（3）每題建立配分與 rubric 條目（各條目配分加總必須等於該題滿分），並為每題撰寫*參考解答*——預設方法會對照參考解答批改，範圍內任何一題缺少參考解答時，系統會直接拒絕啟動批改。]

== 第一階段：校準批

第一批請選 5–10 份代表性答案，而不是直接處理全班。建議包含：

- 一份寫得很好、接近滿分的答案；
- 一份中等答案；
- 一份明顯錯誤或空泛的答案；
- 幾份字跡較不清楚、術語混淆或推導跳躍的答案。

校準批的目的不是快速產出成績，而是確認以下事項：

+ 轉錄是否足夠忠實；
+ 評語的密度與具體程度是否符合教師期待；
+ 同一類錯誤的扣分是否一致；
+ 需要人工複查的情況是否被正確標記；
+ #chg[批改結果與評語的呈現格式是否適合寄給學生。]

#chg[系統內的做法：完成匯入、識別與遮罩審核後，在 Overview 的「Calibrate on a
sample」步驟按下 *Start calibration run*（或在 Runs 頁選 calibration sample
範圍），輸入份數 N——系統會跨題分層、確定性地抽出 N 份答案，*一次 run
完成校準批*（啟動前有成本預覽，抽樣結果記錄於該 run 的項目清單、可重現）。再由助教人工評分數份相同答案，之後在 *Analysis* 分頁的方法卡檢視
AI 與人工的差距（平均差、完全一致率、差距一分以內的比率）。rubric 或提示需要修改時，建立新版本後重跑抽樣即可——舊紀錄連同所用版本一併保留，可直接前後對照。]

== 第二階段：正式批改

#chg[校準完成後，直接對整場考試（assessment 範圍）或單一題（problem 範圍）啟動
run，*一次涵蓋全班*——v1「每批約 10 位、共 18 批」的分批法是 ChatGPT 上傳限制的產物，在 v2 不存在。啟動入口在考試的 Overview 分頁（Start AI grading）或
*Runs* 頁；啟動前系統顯示批改單元數（題數 × 人數）與成本估計，可另設單次 run
成本上限；設定月度預算時，會超出預算的啟動將被拒絕並顯示數字。遮罩審核未全數通過前無法啟動（見第 7 節）。]

== 第三階段：#chg[複核與定案]

#chg[run 完成後在 *Review* 分頁逐題複核：處理強制抽查樣本（見 6.1）、逐一檢視
confidence 為 `low` / `illegible` 或被旗標的個案，必要時填入人工分數——人工分數是「後援」（fallback），在最終成績來源未涵蓋之處生效。若同一批答案跑過多個方法，可在 *Consensus* 分頁做事後聚合（多數決或平均、具容錯，純後處理、不重新呼叫
API）。所有分數在 *Publish* 分頁選定「最終成績來源」（單一 AI run 或 consensus）之後才成為正式成績——這是 v2 的關鍵概念：AI 給分永遠只是候選，正式與否由來源選定與發布動作決定。]

// ============================ 4 ============================
= 評分規準草案：以本題 15 分為例

以下 rubric 可作為校準起點。實際使用前，授課教師可依課堂標準調整。

#table(
  columns: (9.5em, 3em, 1fr),
  stroke: tstroke,
  inset: 6.5pt,
  table.header([*項目*], [*配分*], [*評分重點*]),
  [A. 演算法核心想法正確], [6],
  [正確使用 longest common subsequence 或 LPS dynamic programming，可得高分。若只有反轉字串的想法但未正確連到 subsequence，可部分給分。若使用 longest
   common substring 取代 subsequence，屬核心概念錯誤。],
  [B. 正確性說明], [4],
  [說明為什麼演算法輸出的結果是 longest palindromic subsequence。若只有直覺說法，應部分扣分；若完全沒有 correctness argument，應大幅扣分。],
  [C. 時間複雜度推導], [3],
  [清楚說明 DP table 大小為 $n times n$，因此時間為 $O(n^2)$。只寫出 $O(n^2)$
   但沒有理由時，只能部分給分。],
  [D. 表達與術語精確性], [2],
  [檢查 subsequence、substring、common string、string/string(s) 等用詞。英文單複數或小語病可作評語；若術語混淆影響演算法意義，應扣分。],
)

#callout([本題特別容易出現的錯誤])[
  本題題目要求的是 palindromic subsequence，不是 substring。若學生把問題轉成
  longest common substring，則演算法通常不是本題要求的問題。此錯誤不是單純英文用字問題，而是可能改變演算法內容的概念錯誤。
]

#chg[系統內的對應：把本表建成該題的 rubric 條目（6 ＋ 4 ＋ 3 ＋ 2 ＝ 15，加總必須等於滿分，否則無法儲存）；「特別容易出現的錯誤」這類說明寫進條目描述或參考解答，批改模型與人工複核者都會看到。rubric 修改會產生新版本，舊紀錄記得自己是用哪一版評的。]

// ============================ 5 ============================
= #chg[指令範本：由系統內建模板取代]

#chg[v1 需要在每批上傳時貼上的 prompt 範本*已不再使用*。批改提示是系統內建的
transcribe-then-grade 模板（「提示詞即韌體」，隨批改方法一起版本化），v1 範本的各項規則在 v2 的對應如下：]

#table(
  columns: (1fr, 1.35fr),
  stroke: tstroke,
  inset: 6.5pt,
  table.header([*v1 prompt 規則*], [#chg[*v2 對應*]]),
  [逐字轉錄、不更動用詞、不翻譯、保留錯字與文法錯誤],
  [#chg[內建模板強制逐字轉錄，數學與虛擬碼以 LaTeX 表示；模板明文禁止修正或補完學生內容。]],
  [`\unclear{可能的字}`、`\illegible{}` 逐字標記],
  [#chg[對應為整題的 confidence 標記（`high` / `medium` / `low` / `illegible`）；
   `low` 與 `illegible` 自動列入人工複查對象。（v1 的逐字級標記較細，v2 目前為整題級——這是已知取捨。）]],
  [不要補上學生沒有寫出的內容],
  [#chg[同左，內建於模板；空白或無法辨認一律標 `illegible`、不給推測分。]],
  [XeLaTeX ＋ ctex ＋ base64 內嵌圖片的 `.tex` 輸出],
  [#chg[不適用。系統直接產出結果 PDF（發布信附件），不再經過 `.tex` 編譯。]],
  [像論文 review comments 一樣列出具體問題],
  [#chg[模板要求逐條 criterion 給分並附具體評語；評語隨紀錄保存、可人工修改。]],
  [輸出 `batch_scores.xlsx`、`batch_flags.csv`、zip],
  [#chg[不適用。成績、旗標與複查狀態都在系統內，見第 10 節的匯出方式。]],
)

批改風格的要求不變：不要只寫「不夠完整」；要具體指出哪一步推導錯、哪裡有
logical gap、哪個術語使用錯誤、哪個 claim 沒有 justification、哪裡與題目要求不符，並區分 conceptual error、terminology error、proof gap、complexity
justification issue、minor English/notation issue。#chg[若要讓模型明確依此分類產出評語，可把這段要求寫入自訂方法的提示模板——方法可版本化，並能在
Analysis 中與其他方法並排比較。]

#chg[注意：修改提示模板等同建立新的方法版本，改完應先重跑校準批確認行為，再用於全班。]

// ============================ 6 ============================
= 品質控管建議

== 人工抽查

每批至少抽查 2–3 份。抽查時應看三件事：

+ 原始影像與轉錄文字是否一致；
+ 分數是否符合 rubric；
+ 評語是否具體、可解釋、沒有幫學生補答案。

#chg[v2 把抽查升級為*強制閘門*：以某個 run 作為最終成績來源時，系統會抽出
`min(max(5, 5%), 20)` 份、跨題分層的固定樣本，需在 *Runs* 頁逐份人工複核通過，否則無法大量採納該 run 的分數或發布（管理員可明示豁免）。抽樣是確定性的，重新整理不會換題。]

== 需要標記人工複查的情況

下列情況建議標記為需人工複查：

- 字跡模糊或有多種合理讀法；#chg[（系統以 confidence `low` / `illegible`
  自動標記）]
- 學生答案使用非標準但可能正確的方法；#chg[（在 Review 加旗標並人工給分）]
- 分數落在邊界，例如 6/15 與 7/15、10/15 與 11/15 難以決定；
- 圖片裁切不完整；#chg[（多為掃描歪斜——在識別矩陣檢視原頁即可發現）]
- 答案跨頁或包含未上傳的補充內容；#chg[（*特別注意*：系統目前一個「學生 × 題目」儲存格只容納一頁，同題第二張紙會以
  conflict 停在 Identify 待處理區、不會自動併入，需以並排選擇工具擇一保留——保留任一頁都等於捨棄另一頁。遇到跨頁作答務必人工確認內容完整，再決定給分。）]
- 演算法名詞寫錯但推導可能表示學生其實知道正確概念。

== 分數一致性檢查

所有批次完成後，建議依分數排序，抽查同分或相鄰分數的答案。例如比較所有 7 分、
8 分與 9 分的答案，確認扣分標準一致。若某類錯誤在前後扣分不同，應回頭調整並記錄調整理由。#chg[v2 中 *Analysis* 分頁提供分數分布與方法間比較；同分並排檢視在各題的 Review 清單切換「By
score」——答案依正式分數分組，展開任一分數即並排顯示該分數所有答案的遮罩頁面，點卡片可進入逐題檢視。若調整 rubric 後重跑，舊
run 的紀錄不會被覆蓋，可直接前後對照扣分差異。]

// ============================ 7 ============================
= 隱私與檔案保存

#callout([#chg[去識別化：由系統執行]], tone: R, fillc: rgb("#fdf3f3"))[
  學生考卷屬於敏感教育資料。#chg[v1 要求上傳前手動裁去姓名學號；v2 由系統執行：完成識別（finalize）後，系統自動以學號與姓名欄位的位置預置遮罩框，助教套用並逐頁審核遮罩結果。*遮罩審核未全數通過前，任何 AI run 都無法啟動*；送往視覺模型的一律是遮罩後影像——身分辨識（學號 OCR）與答案批改分離處理，模型永遠不會同時看到「這個人是誰」與「他寫了什麼」。]
]

#chg[學號辨識預設走本機 OCR 模型（伺服器端以 `make ocr-models` 安裝）；「把未匹配的學號欄位送雲端模型辨識」是預設關閉的選項，勾選並明確指定供應商才會送出。兩者都沒有時，頁面進入人工識別佇列，資料完全不出機器。]

#chg[v1 關於 ChatGPT Library 與 30 天檔案保留政策的整段說明*已刪除*——v2 不再把任何考卷資料放進第三方對話服務。批改 API 的輸入依各供應商條款處理，但送出的影像已遮罩、不含學生身分。]

#chg[檔案保存：系統每晚自動備份，順序固定為 blobs（影像）→ 金鑰 → 資料庫傾印，確保備份彼此一致；還原程序與異地副本建議見 `docs/OPERATIONS.md`。系統日誌政策禁止記錄學生姓名、學號、信箱與作答內容。]

// ============================ 8 ============================
= #chg[系統的閘門與資源控管]（取代 v1 的上傳限制）

#chg[v1 的 512 MB／20 MB／每次 10 檔限制不適用：整疊多 GB 的掃描 PDF 可直接上傳，批次大小不再由上傳限制決定。取而代之，v2 用四道閘門控管流程品質：]

+ #chg[*遮罩審核閘門*——遮罩未全數審核通過，不能啟動 AI run；]
+ #chg[*供應商閘門*——被停用的供應商無法啟動 run，避免必然失敗的批次；]
+ #chg[*覆蓋率閘門*——仍有學生未評分或有答案非正式時，不能發布；]
+ #chg[*抽查閘門*——最終來源為方法 run 時，抽查樣本未複核完不能發布（見 6.1）。]

#chg[成本煞車有兩道：單次 run 可設成本上限；另可設定全系統月度預算，估計後會超出預算的啟動直接被拒絕。模型單價由操作者在 Providers 頁維護，成本估算與月報表都以它為準。]

#chg[掃描品質提醒：若頁面的文字未能存留於渲染影像（常見於未內嵌的中文字型），系統會對該頁提出警示——AI 批改的是渲染後影像，請比對原始檔並重新匯出或重掃。]

#callout([建議設定])[
  - 校準批：5–10 位學生。
  - #chg[正式批改：全班一次 run（assessment 或 problem 範圍），啟動前看成本預覽。]
  - #chg[run 完成後：立即處理抽查樣本與 `low` / `illegible` 個案。]
  - #chg[發布前：選定最終成績來源，檢查 Totals 與分數分布。]
]

// ============================ 9 ============================
= 助教操作清單

#table(
  columns: (7.5em, 1fr),
  stroke: tstroke,
  inset: 6.5pt,
  table.header([*階段*], [*檢查事項*]),
  [#chg[開始前]],
  [#chg[roster 匯入（`student_id,name,email`）；三個辨識欄位區域已畫定（務必上傳前畫好——之後修改不會重跑已辨識的頁面，finalize 後舊遮罩框也會殘留）；
   Providers／Methods／每題 rubric 與參考解答就緒。]],
  [#chg[上傳與識別]],
  [#chg[整疊上傳；在人工識別佇列逐頁確認（鍵盤 j / k / Enter，只能從名冊搜尋選人、不能自由輸入）；檢查「學生 × 題目」矩陣的缺格；finalize 可增量執行，缺格需明確確認。]],
  [校準批],
  [檢查轉錄保真度、評語密度、扣分標準#chg[、複查標記]。必要時請教師調整
   rubric#chg[（產生新版本後重跑校準批）]。],
  [#chg[正式批改]],
  [#chg[一次 run 全班；核對成本預覽與上限；run 進度在 Runs 頁追蹤，失敗單元可重試。]],
  [#chg[run 完成後]],
  [#chg[完成強制抽查樣本；複查 `low` / `illegible` 與旗標個案；必要處填入人工後援分數。]],
  [發布前],
  [#chg[在 Publish 選定最終成績來源；檢查 Totals 與分數分布；發布對話框會顯示寄件地址，確認無誤後送出。]每位學生只會收到自己的批改結果——#chg[這在 v2
   是系統結構性保證（逐生快照信件），不再靠人工核對附件。]],
  [#chg[發布後]],
  [#chg[學生直接回覆結果信、以 `<p1>…</p1>` 標籤指明題號申請 regrade；助教在
   Regrade inbox 逐題裁決（可用 AI 複改輔助，僅供助教參考）；期限與輪次在
   Regrade rounds 分頁控管；格式完全錯誤的回信不會消耗學生的申覆次數。]],
)

// ============================ 10 ============================
= #chg[檔案結構：系統儲存與助教留存]

#chg[v1 的 `course_grading_Q5/` 目錄樹（18 個 batch 資料夾、逐批 zip 與成績表）
*不再需要維護*：原始頁、遮罩影像、轉錄、給分、評語與發布快照全部由系統儲存並納入每晚備份。助教本機只需保留：]

#align(center)[
```
course_grading/
├── scans/            原始掃描檔（上傳後留作備援）
│   └── final_exam_pile.pdf
└── roster/
    └── roster.csv    名冊（student_id,name,email）
```
]

#chg[需要交付或存檔時，由系統匯出：每位學生的結果 PDF（發布信附件，選項三種：不附檔、壓縮或原始品質；另可勾選改寄逐題 JPEG 的 zip，作為信箱附件大小限制的後備）、全班成績匯出（含退選標記）、以及單一學生的補寄（individual resend）。]

// ============================ 11 ============================
= 建議時程

#table(
  columns: (6.5em, 1fr),
  stroke: tstroke,
  inset: 6.5pt,
  table.header([*時間*], [*工作*]),
  [第 0 天], [#chg[掃描全班考卷；匯入 roster；畫定三個辨識欄位區域。]],
  [第 1 天], [#chg[上傳整疊、清人工識別佇列、finalize、遮罩審核；跑 5–10 份校準批；教師檢查並修正 rubric。]],
  [第 2 天], [#chg[正式 run 全班；完成強制抽查樣本與 Review 複核、補人工後援分數。]],
  [第 3 天], [#chg[選定最終成績來源；檢查 Totals 與分數分布；發布並寄送結果
   PDF。]],
  [#chg[發布後]], [#chg[依 Regrade rounds 設定受理學生回信申覆；到期後由各題負責助教收尾。]],
)

#chg[較 v1 的 5 天時程縮短，主因是「分批上傳、逐批下載 zip、合併 18 份成績表」的人工步驟全部消失；多出來的時間應花在抽查與邊界個案上。]

// ============================ 12 ============================
= 資料來源

#chg[本文件依 AdaGrade 專案文件與系統內建說明整理（部分沿用舊名 ADA-Marker）：]

+ #chg[系統內建使用指南（登入後左側 Guide 頁）——各分頁與控制項的完整說明；]
+ #chg[`docs/OPERATIONS.md`——備份、還原與維運程序；]
+ #chg[`docs/TA_DEPLOYMENT.md`——免 root 權限的工作站部署（Docker Postgres ＋
  Cloudflare Tunnel）；]
+ #chg[`docs/DECISIONS.md`——系統設計決策（D1–D68），含遮罩不變式、發布快照、
  regrade 規則等本文引用的機制。]

#chg[v1 引用的 OpenAI Help Center 連結（上傳限制、Library 與檔案保留政策）已不再適用，僅存於 v1 文件供歷史參考。]

// ============================ 附錄 ============================
#pagebreak(weak: true)
#heading(level: 1, numbering: none)[#chg[附錄：v1 → v2 變更總覽]]

#chg[_v2.1（2026-07-20）：系統更名 AdaGrade（原 ADA-Marker）；v1 文件移至
`docs/ta_batch_grading_guide_v1.pdf`；修正三處描述——結果 PDF 附件選項、跨頁答案的處理（conflict 而非
duplicate）、「修改辨識欄位區域的影響」；原列為「規劃中」的兩項功能上線——校準批一鍵抽樣
N 份（3.1 節）與同分並排檢視（6.3 節）。紅字仍表示相對 v1.0 的變更。_]

#chg[#table(
  columns: (6.5em, 1fr, 1.15fr),
  stroke: 0.4pt + rgb("#d9a0a0"),
  inset: 6.5pt,
  table.header([*面向*], [*v1（ChatGPT 網頁）*], [*v2（AdaGrade）*]),
  [批改引擎], [ChatGPT 網頁對話，逐批上傳], [自架系統直連批改 API，資料不進第三方介面],
  [匿名化], [人工裁切＋匿名編號對照表], [系統遮罩＋身分/內容分離，遮罩審核閘門強制],
  [分批], [每批約 10 份、共 18 批], [全班一次 run；由四道閘門與成本煞車控管],
  [提示詞], [每批貼範本、人工維護], [內建 transcribe-then-grade 模板，隨方法版本化],
  [輸出], [.tex／.pdf／xlsx／csv／zip 手動合併], [系統儲存全部紀錄；發布信附結果 PDF、成績可匯出],
  [抽查], [自律建議：每批 2–3 份], [強制閘門：min(max(5, 5%), 20) 跨題分層樣本],
  [複查標記], [needs_human_review 欄位], [confidence 自動標記＋旗標＋人工後援分數],
  [發布], [人工逐一寄送、自行核對], [快照發布、逐生信件結構性隔離、可個別補寄],
  [申覆], [（未涵蓋）], [學生回信 `<p1>` 標籤申覆，輪次與期限內建],
)]
